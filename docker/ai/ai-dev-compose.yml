services:
  ai-service:
    image: alpine:latest
    command: tail -f /dev/null
    models:
      - phi3
    ports:
      - "11434:11434"
    networks:
      - internal

networks:
  internal:
    driver: bridge

models:
  phi3:
    model: huggingface/Phi-3-mini-4k-instruct-gguf
